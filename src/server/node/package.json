{
  "name": "savage",
  "version": "0.0.1",
  "description": "Wraps a BDO's svg property in a minimal HTML page with it set as the og:image, for shareable link previews",
  "main": "savage.js",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/freyja-love-and-magic/savage.git"
  },
  "scripts": {
    "start": "node savage.js",
    "test:sanitize": "node scripts/test-sanitize.mjs",
    "test:vcard": "node scripts/test-vcard.mjs",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "teleportation-js": "latest"
  },
  "//": "sessionless-node is a devDependency because scripts/*.mjs sign their own test requests. It currently also resolves as a transitive dep of teleportation-js; declaring it keeps the tests working if that ever changes. (npm reads every key inside a dependency block as a package name, so this note has to live at the top level — '//' is npm's own convention for it.)",
  "devDependencies": {
    "sessionless-node": "^0.11.0"
  }
}
